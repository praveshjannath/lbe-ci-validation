name: v2-release-candidate

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "pyproject.toml"
      - "lbe_guard_inspector/**"
      - "npm/**"
      - ".github/workflows/v2-release-candidate.yml"

permissions:
  contents: read

jobs:
  release-candidate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Install Python build/test dependencies
        run: |
          python -m pip install --upgrade pip "setuptools>=75"
          python -m pip install .[test]

      - name: Verify V2.0.1 package versions
        run: |
          python - <<'PY'
          import json
          import tomllib
          from pathlib import Path
          py = tomllib.loads(Path("pyproject.toml").read_text(encoding="utf-8"))
          npm = json.loads(Path("npm/package.json").read_text(encoding="utf-8"))
          assert py["project"]["name"] == "lbe-guard-inspector"
          assert py["project"]["version"] == "2.0.1"
          assert npm["name"] == "@letterblack/lbe"
          assert npm["version"] == "2.0.1"
          print("V2_0_1_VERSION_PAIR=PASS")
          PY
          node -e "const r=require('./npm/lib/runtime-install'); if(r.PUBLIC_PYTHON_PACKAGE_VERSION!=='2.0.1') process.exit(1); console.log('PUBLIC_RUNTIME_PIN=PASS')"

      - name: Run Python suite
        run: python -m pytest -q

      - name: Build Python release artifacts
        run: python -m build

      - name: Verify Python wheel and sdist names
        shell: bash
        run: |
          test -f dist/lbe_guard_inspector-2.0.1-py3-none-any.whl
          test -f dist/lbe_guard_inspector-2.0.1.tar.gz
          echo "PYTHON_V2_0_1_ARTIFACTS=PASS"

      - name: Run npm bootstrap tests
        working-directory: npm
        run: npm test

      - name: Audit npm V2.0.1 tarball
        working-directory: npm
        run: |
          node -e "const p=require('./package.json'); if(p.name!=='@letterblack/lbe'||p.version!=='2.0.1') process.exit(1)"
          npm pack --dry-run --json > npm-pack.json
          node - <<'NODE'
          const fs = require('node:fs');
          const pack = JSON.parse(fs.readFileSync('npm-pack.json','utf8'))[0];
          const expected = [
            'README.md',
            'bin/lbe.js',
            'lib/launcher.js',
            'lib/paths.js',
            'lib/python-discovery.js',
            'lib/runtime-discovery.js',
            'lib/runtime-install.js',
            'package.json'
          ];
          const actual = pack.files.map(x => x.path);
          if (JSON.stringify(actual) !== JSON.stringify(expected)) {
            console.error({actual, expected});
            process.exit(1);
          }
          console.log('NPM_V2_0_1_TARBALL_ALLOWLIST=PASS');
          NODE
          rm npm-pack.json
          npm pack --json

      - name: Upload Python V2.0.1 artifacts
        uses: actions/upload-artifact@v4
        with:
          name: lbe-python-v2.0.1
          path: |
            dist/lbe_guard_inspector-2.0.1-py3-none-any.whl
            dist/lbe_guard_inspector-2.0.1.tar.gz

      - name: Upload npm V2.0.1 tarball
        uses: actions/upload-artifact@v4
        with:
          name: lbe-npm-v2.0.1
          path: npm/letterblack-lbe-2.0.1.tgz
